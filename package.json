{
  "name": "svelte-app",
  "version": "0.0.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "prettier --ignore-path .gitignore --check --plugin-search-dir=. . && eslint --ignore-path .gitignore .",
    "format": "prettier --ignore-path .gitignore --write --plugin-search-dir=. .",
    "sync": "dotenv -- cld config && dotenv -- cld sync --push static static"
  },
  "devDependencies": {
    "@beyonk/svelte-carousel": "^2.8.0",
    "@sveltejs/adapter-vercel": "next",
    "@sveltejs/kit": "1.30.4",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-svelte3": "^3.2.1",
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0",
    "svelte": "^3.42.6",
    "svelte-feather-icons": "^3.3.0",
    "svelte-i18n": "^4.0.0",
    "vite": "^5.2.11"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^10.0.0",
    "dotenv-cli": "^4.1.1",
    "svelte-lazy": "^1.0.12",
    "svelte-viewport-info": "^0.1.12"
  }
}
